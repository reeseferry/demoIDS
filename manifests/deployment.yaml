# Generated by IBM TransformationAdvisor
# Thu Mar 12 19:36:13 UTC 2020

apiVersion: v1
kind: Service
metadata:
  name: idswebapp
  labels:
    app: idswebapp
spec:
  ports:
    - port: 9080
  selector:
    app: idswebapp
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: idswebapp
  name: idswebapp
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: idswebapp
    spec:
      containers:
      - image: mycluster.icp:8500/default/idswebapp:latest
        name: idswebapp
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
